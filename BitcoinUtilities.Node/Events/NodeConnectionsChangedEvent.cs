using BitcoinUtilities.Threading;

namespace BitcoinUtilities.Node.Events
{
    /// <summary>
    /// Raised when a peer is connected to the node or when a peer is disconnected from the node.
    /// </summary>
    public sealed class NodeConnectionsChangedEvent : IEvent
    {
    }
}