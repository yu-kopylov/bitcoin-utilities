namespace BitcoinUtilities
{
    /// <summary>
    /// Functions that behave differently for main network and test network use this enum as an input parameter.
    /// </summary>
    public enum BitcoinNetworkKind
    {
        Main,
        Test
    }
}